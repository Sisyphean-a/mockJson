# Attention

- 本项目是 Windows 本地使用的通用 Mock / Proxy 控制台；核心主线是按接口匹配并进行多场景 Mock，UI 仅参考外部项目 `E:\其他\Apply this change`，不得复制其 mock 数据。
- 加载范围前先看本文件和 `architecture/INDEX.md`；已确认的产品主线与领域规则见 `requirements/CONTEXT.md`。
- `.tmp/` 已由根 `.gitignore` 忽略，外部参考素材和中间文件只能放入该目录。
