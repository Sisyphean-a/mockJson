# history 2026-09

- 2026-09-09 · [功能] 为 Package 增加可切换的多环境真实服务配置。范围：package:mock-console
  原因：测试环境和生产环境可以复用同一套 Logical API 与 Scenario，不应为了不同真实域名复制 Package；用户需要在请求未命中 Mock 时直接切换转发环境。
  当前依据：`.codestable/requirements/CONTEXT.md`、`.codestable/architecture/packages/mock-console.md`。
  证据：`packages/mock-console/shared/types.ts`、`packages/mock-console/server/src/config-service.ts`、`packages/mock-console/server/src/proxy.ts`、`packages/mock-console/web/src/components/PackageBar.vue`、`packages/mock-console/web/src/components/ConsoleModals.vue`；旧 `targetBaseUrl` 配置读取时迁移为“默认服务”，新增服务管理与代理切换测试，`npm test`、`npm run typecheck`、`npm run build`。

- 2026-09-09 · [功能] 调整新建 Logical API、匹配方式和首个 Scenario 的默认状态。范围：package:mock-console
  原因：控制台新建接口应立即可用，多个匹配条件默认满足任一；接口没有场景时首个响应应直接作为当前返回，减少新建后的额外启用操作，同时已有场景新增仍不切换当前响应。
  当前依据：`.codestable/requirements/CONTEXT.md`。
  证据：`packages/mock-console/server/src/config-service.ts`、`packages/mock-console/web/src/use-scenario-actions.ts`、`packages/mock-console/server/src/config-service.test.ts`、`README.md`。

- 2026-09-09 · [重构] 将 Chrome 扩展与 Mock Console 之间的线协议抽成独立共享契约包，并按 workspace 拆分 CodeStable 架构页面。范围：workspace|package:mock-console|package:chrome-extension|package:extension-contract|shared:extension-runtime
  原因：扩展此前通过相对路径直接导入 `packages/mock-console/shared/types.ts`，产品包边界与架构文档范围不一致；共享 DTO 应由无运行时副作用的契约包唯一拥有，两个产品包只依赖公开包名。
  当前依据：`.codestable/architecture/INDEX.md`、`.codestable/architecture/packages/`、`.codestable/architecture/shared/extension-runtime.md`、`.codestable/requirements/CONTEXT.md`。
  证据：`packages/extension-contract/src/index.ts`、两个产品包的 package.json、扩展与 resolver 的 imports；架构索引改为范围地图并新增包级页面和共享机制页；旧内部相对路径引用为 0，`npm test` 79/79、`npm run typecheck`、`npm run build`。

- 2026-09-09 · [缺陷] 修复 Chrome 扩展影响第三方页面上传。范围：package:chrome-extension
  原因：拦截器先用 `new Request(input, init)`读取请求元数据，随后未命中、超时或异常分支仍以原始参数调用原生 fetch；当页面传入 `Request` / `ReadableStream` 上传体时，原请求体可能已被转移或消费。此前白名单只要非空就会在所有页面安装 XHR Proxy，即使页面自身不在白名单也会改变第三方站点的 XHR 语义；multipart、图片和其他二进制上传还不应进入 JSON resolver。
  当前依据：`.codestable/requirements/CONTEXT.md`。
  证据：`packages/chrome-extension/src/content-main.ts`、`packages/chrome-extension/src/content-main.test.ts`；放行分支复用已构造的 `Request`，无关页面保持原生 XHR，上传体直接放行，并覆盖 pass/timeout/exception、FormData、Blob 和 ReadableStream；`npm test`、`npm run typecheck`、`npm run build`。

- 2026-09-09 · [缺陷] 修复 Chrome 扩展 resolver 双重 200ms 超时竞态与缺失结果读取。范围：package:chrome-extension
  原因：Service Worker 在 200ms 中止本机 resolver 请求，页面 MAIN world 也在同一时间点 fail-open；服务端可能已经记录 `mocked`，但页面先收到放行或继续真实请求，形成后台日志与页面结果不一致。桥接结果异常时 MAIN world 还直接读取 `action`，会产生未捕获 Promise 错误；同时，Mock Console 不可用时每个后续请求都会重复等待完整超时，取消请求还会继续占用扩展链路。
  当前依据：`.codestable/requirements/CONTEXT.md`、`README.md`。
  证据：`packages/chrome-extension/src/protocol.ts`、`packages/chrome-extension/src/service-worker.ts`、`packages/chrome-extension/src/resolver-health.ts`、`packages/chrome-extension/src/content-main.ts`、`packages/chrome-extension/src/content-bridge.ts`、`packages/chrome-extension/src/content-main.test.ts`、`packages/chrome-extension/src/extension-chain.test.ts`、`packages/chrome-extension/src/resolver-health.test.ts`；Service Worker 超时调整为 1 秒，页面桥接为 1.2 秒，缺失结果显式放行，连续两次 resolver 失败后短路 3 秒并保存到当前扩展会话，同时最多处理 32 个 resolver，取消会传播到 Service Worker，监控状态增加启动握手，Mock XHR 延迟支持取消；`npm run typecheck`、`npm test` 74/74、`npm run build`。

- 2026-09-09 · [体验] 将请求日志改为独立全宽观测视图，并修复日志地址展示与接口跳转。范围：package:mock-console/web
  原因：日志页同时显示 Logical API 配置侧栏造成两个导航上下文，点击接口没有可见效果；扩展日志的完整 URL 还与 host 重复拼接，详情标题也把逻辑接口误当成请求主体。
  当前依据：`.codestable/requirements/CONTEXT.md`、`README.md`。
  证据：`packages/mock-console/web/src/App.vue`、`packages/mock-console/web/src/components/RequestLogPanel.vue`、`packages/mock-console/web/src/styles/shell.css`、`packages/mock-console/web/src/styles/logs.css`；浏览器日志视图快照确认无配置侧栏，点击「查看接口配置」可回到对应接口；`npm run typecheck`、`npm test`、`npm run build`、`git diff --check`。

- 2026-09-09 · [功能] 为控制台增加 Logical API / Scenario 拖拽排序和 Chrome 扩展来源日志。范围：workspace|package:mock-console
  原因：管理页面需要以长按拖拽方式调整两个列表的 UI 顺序，同时请求日志需要区分 Proxy / Reqable 与 Chrome 扩展判定；扩展未命中仍由浏览器原生请求放行，不回退到 Mock Console 代理。
  当前依据：`.codestable/requirements/CONTEXT.md`、`README.md`。
  证据：`vue-draggable-plus`、`packages/mock-console/server/src/config-service.ts`、`packages/mock-console/server/src/extension-routes.ts`、`packages/mock-console/web/src/components/ApiSidebar.vue`、`packages/mock-console/web/src/components/ScenarioPanel.vue`、`packages/mock-console/web/src/components/RequestLogPanel.vue`；`npm run typecheck`、`npm test`、`npm run build`。

- 2026-09-09 · [缺陷] 修复 Chrome 扩展 MAIN world 内容脚本保留运行时模块导入导致的拦截失效。范围：package:chrome-extension
  原因：Vite 构建的 `content-main.js` 曾保留对白名单共享 chunk 的 `import`，但 Manifest 的 `content_scripts` 按普通脚本注入，浏览器报 `Cannot use import statement outside a module`，导致页面 fetch / 异步 XHR 拦截器完全没有执行；白名单和服务端 matcher 本身仍可正常命中。
  当前依据：`.codestable/requirements/CONTEXT.md`。
  证据：`packages/chrome-extension/src/content-main.ts`、`packages/chrome-extension/dist/content-main.js`；`npm run typecheck`、`npm test` 65/65、`npm run build:extension`，构建后 `content-main.js` 无静态 import；Vite 构建钩子会持续校验该约束。

- 2026-09-08 · [功能] 为匹配条件操作列增加编辑入口。范围：package:mock-console/web
  原因：条件新增后只有删除操作，无法修正 URL、Header 或 Method 的字段、运算符和值；编辑回填原规则并通过现有 Logical API 更新接口，保留规则 ID。
  当前依据：`.codestable/requirements/CONTEXT.md`、`README.md`。
  证据：`packages/mock-console/web/src/components/MatchPanel.vue`、`packages/mock-console/web/src/use-api-actions.ts`、`packages/mock-console/web/src/use-console-forms.ts`；`npm run typecheck`、`npm test`、`npm run build`。

- 2026-09-08 · [体验] 收敛 Mock Console 服务端终端日志，只保留启动、错误和安全拒绝信息。范围：package:mock-console/server
  原因：Fastify 默认的 `incoming request` / `request completed` 与扩展 resolver 的逐请求访问日志没有业务信息，会在页面轮询或接口频繁调用时刷屏；请求匹配结果仍由控制台日志面板保留。
  当前依据：`.codestable/requirements/CONTEXT.md`、`README.md`。
  证据：`packages/mock-console/server/src/index.ts`；`npm run typecheck`、`npm test`、`npm run build`。

- 2026-09-08 · [功能] 为 Chrome 扩展增加请求域名白名单，避免默认对所有网页接口发起 Mock 判定。范围：package:chrome-extension
  原因：扩展脚本仍可静态注入所有 HTTP/HTTPS 页面，但只有 Popup 配置的请求目标域名才启用 fetch / 异步 XHR 判定；新安装默认包含 `localhost` 和 `127.0.0.1`，白名单为空或请求域名未命中时不发送 resolver 请求，从源头减少无关请求和服务端日志。
  当前依据：`.codestable/architecture/INDEX.md`、`.codestable/requirements/CONTEXT.md`。
  证据：`packages/chrome-extension/src/domain-whitelist.ts`、`packages/chrome-extension/src/content-main.ts`、`packages/chrome-extension/src/content-bridge.ts`、`packages/chrome-extension/src/service-worker.ts`、`packages/chrome-extension/src/popup.ts`；`npm test` 63/63、`npm run typecheck`、`npm run build`、`git diff --check`。

- 2026-09-08 · [功能] 增加 Chrome 扩展 Popup，支持全局 Mock、当前标签页 Mock 和本机服务状态查看。范围：package:chrome-extension
  原因：扩展初版没有显式启停入口，用户无法在不修改 Mock 配置的情况下暂停请求拦截；开关只影响扩展运行态，并以会话存储保持跨 Service Worker 生命周期的一致性。
  当前依据：`.codestable/architecture/INDEX.md`、`.codestable/requirements/CONTEXT.md`。
  证据：`packages/chrome-extension/src/popup.ts`、`packages/chrome-extension/src/service-worker.ts`、`packages/chrome-extension/public/manifest.json`、`packages/mock-console/server/src/extension-routes.ts`；`npm test` 60/60、`npm run typecheck`、`npm run build`。

- 2026-09-08 · [功能] 增加 Chrome 扩展运行时 Mock，并将仓库拆分为 `mock-console` 与 `chrome-extension` 两个 npm workspace。范围：workspace|package:mock-console|package:chrome-extension
  原因：页面 fetch / 异步 XMLHttpRequest 需要复用现有接口、优先级和场景规则，但未命中必须由浏览器原生请求直接放行；扩展不应复制或持久化 Mock 配置。目录边界同时明确控制台服务与浏览器桥接的独立发布和安全边界。
  当前依据：`.codestable/architecture/INDEX.md`、`.codestable/requirements/CONTEXT.md`。
  证据：`packages/mock-console/server/src/runtime-resolver.ts`、`packages/mock-console/server/src/extension-routes.ts`、`packages/chrome-extension/src/`、`package.json`；`npm test` 59/59、`npm run typecheck`、`npm run build:mock-console`、`npm run build:extension`。

- 2026-09-07 · [重构] 优化大 JSON 编辑、配置保存、请求日志同步和接口匹配的重复开销。范围：workspace
  原因：光标移动不应重复解析全文；配置保存不应长期保留整份对象回滚副本；日志轮询在有流量时不应反复传输完整列表；固定日志上限应使用环形写入，接口匹配应复用按优先级排序结果；有请求体的透明代理不应先被 Fastify 完整缓冲，同时保持原子写入、失败回滚、日志上限和同优先级顺序。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/web/src/components/JsonEditor.vue`、`apps/server/src/config-service.ts`、`apps/server/src/storage.ts`、`apps/server/src/request-logs.ts`、`apps/server/src/admin-routes.ts`、`apps/web/src/mock-admin-client.ts`、`apps/server/src/proxy.ts`；`npm run typecheck`、`npm test` 52/52、`npm run build`。

- 2026-09-04 · [功能] 增加本地请求日志视图，记录 Mock 命中的接口/场景、转发结果、状态、耗时和响应数据预览，并支持轮询、筛选、详情查看与清空。范围：workspace
  原因：用户需要直接确认请求命中了哪个逻辑接口以及实际返回了什么；日志保持运行态内存数据，不污染配置文件，也不扩展成通用抓包分析器。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/request-logs.ts`、`apps/server/src/proxy.ts`、`apps/server/src/admin-routes.ts`、`apps/web/src/components/RequestLogPanel.vue`；`npm test`、`npm run typecheck`、`npm run build`。

- 2026-09-16 · [缺陷] 修复生产控制台无法连接管理 API、开发页面依赖 Vite 代理，以及从其他目录启动时找不到构建产物的问题。范围：workspace
  原因：管理客户端把原生 `fetch` 保存为实例字段后以客户端实例作为接收者调用，Chrome 抛出 `Illegal invocation`，导致生产页面的管理请求无法发出；开发页面原先还对 `22334` 使用相对 URL，服务端则用 `process.cwd()` 查找 `dist`。现在客户端以全局对象作为 `fetch` 接收者，开发页面固定直连 `127.0.0.1:22333`，正式根页面禁止缓存旧入口 HTML，并按源码位置解析项目根目录。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/web/src/mock-admin-client.ts`、`apps/server/src/index.ts`、`apps/web/src/mock-admin-client.test.ts`；生产构建经专用浏览器加载现有测试包与接口；`npm run typecheck`、`npm test` 30/30、`npm run build`。

- 2026-09-16 · [重构] 将 Mock 配置业务、管理路由、前端状态、表单、领域动作和视图区域按变化原因拆分。范围：workspace
  原因：原实现由 `index.ts` 直接编排全部管理状态变更，由 `useMockConsole.ts` 同时承载 HTTP、状态、表单和所有 CRUD，`App.vue` 还包含整页交互模板；重构后保持管理 API、代理行为和页面流程不变，并让业务规则获得唯一所有者。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/config-service.ts`、`apps/server/src/admin-routes.ts`、`apps/shared/types.ts`、`apps/web/src/mock-admin-client.ts`、`apps/web/src/use-mock-state.ts`、`apps/web/src/components/`；`npm test` 27/27、`npm run typecheck`、`npm run build`。

- 2026-09-16 · [演进] 确认项目主线为通用的多场景接口 Mock / Proxy 工具，不限定 Android 或测试人员。范围：workspace
  原因：用户确认核心思路是按接口匹配并在多个 Mock 场景之间切换，使用者和客户端类型可以多样化。
  当前依据：`requirements/CONTEXT.md`、`architecture/INDEX.md`。
  证据：本轮用户确认与修正。

- 2026-09-16 · [演进] 移除已废弃的详细设计文档，产品主线改由 `cs-goal` 独立确认。范围：workspace
  原因：用户明确该文档不再作为后续依据；避免未来把旧提案误当成最终产品契约。
  当前依据：`requirements/CONTEXT.md`、当前实现。
  证据：删除 `Mock控制台_项目详细设计.md`；本轮用户决定。

- 2026-09-16 · [缺陷] 修复窄屏接口列表的图标换行与内容区横向溢出，并补齐 HTTPS 真实上游代理。范围：workspace
  原因：固定侧栏在窄屏下挤压接口图标和主内容；代理使用 HTTP 客户端无法处理设计文档允许的 HTTPS targetBaseUrl。Header、URL 和 Reqable 使用语义同时在表单中明确。
  当前依据：`requirements/CONTEXT.md`。
  证据：`apps/web/src/App.vue`、`apps/server/src/proxy.ts`、`apps/server/src/proxy.test.ts`。

- 2026-09-16 · [演进] 统一本地运行端口为 22333，开发热更新端口为 22334。范围：workspace
  原因：避免使用常见的 5173 和 8080；正式运行时页面、管理 API 与 Mock / Proxy 共用一个入口，真实上游地址不再默认指向本地 Mock 端口。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/index.ts`、`apps/web/vite.config.ts`、`data/mock-data.json`、`README.md`。

- 2026-09-16 · [功能] 初始化 Mock / Proxy 控制台首版闭环。范围：workspace
  原因：按项目详细设计建立本地 Package、Logical API、Match Rule、Scenario 模型，提供真实管理 API、场景切换、JSON 校验、透明转发和 Vue 控制台；初始数据保持为空，不沿用 UI 参考项目 mock 数据。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/index.ts`、`apps/server/src/matcher.ts`、`apps/server/src/proxy.ts`、`apps/web/src/App.vue`、`data/mock-data.json`。

- 2026-09-16 · [演进] 初始化 `.codestable/` 项目记忆（attention、架构索引、领域上下文索引、history）。范围：workspace
  原因：仓库为全新空仓库，无代码、无提交，需要为后续会话提供最小且一致的加载入口，避免从目录名臆测用途。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`（均为空态索引）。
  证据：`.codestable/` 目录本身；Git 仓库无任何提交。

- 2026-09-04 · [重构] 深化前端管理请求边界并统一连接状态、运行地址和启动错误展示。范围：workspace
  原因：原生 `fetch` 曾作为实例字段改变调用接收者，任意 `TypeError` 又被客户端回调误判为断线；连接状态、端口规则和启动错误分散在多层，导致编程错误被包装成环境故障。现在浏览器传输负责调用语义，状态层唯一维护管理请求可用状态，运行地址只有一个策略来源，启动错误只有一个展示入口，服务日志明确区分 ui、admin 和 proxy。
  当前依据：`architecture/INDEX.md`。
  证据：`apps/web/src/runtime-endpoints.ts`、`apps/web/src/mock-admin-client.ts`、`apps/web/src/use-mock-state.ts`、`apps/web/src/App.vue`、`apps/server/src/index.ts`；`npm test` 37/37、`npm run typecheck`、`npm run build`、隔离端口 22335 生产页面与请求通道验证。

- 2026-09-04 · [演进] 将运行态 Mock 配置移出 Git 仓库，并保留脱敏示例文件。范围：workspace
  原因：`data/mock-data.json` 会持久化用户配置和完整响应，进入 Git 后可能泄露业务数据或改变代理目标；运行态文件应由本机用户数据目录管理，仓库只保存可公开的示例。
  当前依据：`architecture/INDEX.md`、`README.md`。
  证据：`apps/server/src/state-path.ts`、`apps/server/src/index.ts`、`.gitignore`、`data/mock-data.example.json`。

- 2026-09-04 · [缺陷] 补齐 Mock / Proxy 产品主线并修复场景切换、请求体转发、配置恢复和管理边界。范围：workspace
  原因：控制台已有首版页面但 Package、接口和场景维护不完整，无请求体 POST 被错误 JSON 头拦截，JSON 代理丢失请求体，未完成接口会遮挡可用 Mock，损坏配置可能被静默清空。
  当前依据：`requirements/CONTEXT.md`、`architecture/INDEX.md`。
  证据：`apps/server/src/index.ts`、`apps/server/src/proxy.ts`、`apps/server/src/storage.ts`、`apps/server/src/validation.ts`、`apps/web/src/useMockConsole.ts`、`apps/web/src/App.vue`、服务端回归测试。

- 2026-09-04 · [重构] 将前端单文件组件拆分为页面、状态行为和样式三个职责。范围：workspace
  原因：`App.vue` 超过一千行，状态转换、请求副作用、模板和样式混在同一文件，增加维护和审查成本。
  当前依据：`architecture/INDEX.md`。
  证据：`apps/web/src/App.vue`、`apps/web/src/useMockConsole.ts`、`apps/web/src/App.css`。

- 2026-09-04 · [演进] 场景编辑与生效分离，并明确零规则接口不处理请求。范围：workspace
  原因：用户确认产品只处理匹配到的请求；新场景在完整配置前不得接管响应，零规则也不能隐式成为全量兜底。
  当前依据：`requirements/CONTEXT.md`。
  证据：`apps/server/src/matcher.ts`、`apps/server/src/proxy.ts`、`apps/server/src/index.ts`、`apps/web/src/useMockConsole.ts`、`apps/web/src/App.vue`、本轮用户决定。
