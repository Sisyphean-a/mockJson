# history 2026-09

- 2026-09-16 · [演进] 确认项目主线为通用的多场景接口 Mock / Proxy 工具，不限定 Android 或测试人员。范围：workspace
  原因：用户确认核心思路是按接口匹配并在多个 Mock 场景之间切换，使用者和客户端类型可以多样化。
  当前依据：`requirements/CONTEXT.md`、`architecture/INDEX.md`。
  证据：本轮用户确认与修正。

- 2026-09-16 · [演进] 移除已废弃的详细设计文档，产品主线改由 `cs-goal` 独立确认。范围：workspace
  原因：用户明确该文档不再作为后续依据；避免未来把旧提案误当成最终产品契约。
  当前依据：`requirements/CONTEXT.md`、当前实现。
  证据：删除 `Mock控制台_项目详细设计.md`；本轮用户决定。

- 2026-09-16 · [缺陷] 修复窄屏接口列表的图标换行与内容区横向溢出，并补齐 HTTPS 真实上游代理。范围：workspace
  原因：固定侧栏在窄屏下挤压接口图标和主内容；代理使用 HTTP 客户端无法处理设计文档允许的 HTTPS targetBaseUrl。Header、URL 和 Reqable 使用语义同时在表单中明确。
  当前依据：`requirements/CONTEXT.md`。
  证据：`apps/web/src/App.vue`、`apps/server/src/proxy.ts`、`apps/server/src/proxy.test.ts`。

- 2026-09-16 · [演进] 统一本地运行端口为 22333，开发热更新端口为 22334。范围：workspace
  原因：避免使用常见的 5173 和 8080；正式运行时页面、管理 API 与 Mock / Proxy 共用一个入口，真实上游地址不再默认指向本地 Mock 端口。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/index.ts`、`apps/web/vite.config.ts`、`data/mock-data.json`、`README.md`。

- 2026-09-16 · [功能] 初始化 Mock / Proxy 控制台首版闭环。范围：workspace
  原因：按项目详细设计建立本地 Package、Logical API、Match Rule、Scenario 模型，提供真实管理 API、场景切换、JSON 校验、透明转发和 Vue 控制台；初始数据保持为空，不沿用 UI 参考项目 mock 数据。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`。
  证据：`apps/server/src/index.ts`、`apps/server/src/matcher.ts`、`apps/server/src/proxy.ts`、`apps/web/src/App.vue`、`data/mock-data.json`。

- 2026-09-16 · [演进] 初始化 `.codestable/` 项目记忆（attention、架构索引、领域上下文索引、history）。范围：workspace
  原因：仓库为全新空仓库，无代码、无提交，需要为后续会话提供最小且一致的加载入口，避免从目录名臆测用途。
  当前依据：`architecture/INDEX.md`、`requirements/CONTEXT.md`（均为空态索引）。
  证据：`.codestable/` 目录本身；Git 仓库无任何提交。
